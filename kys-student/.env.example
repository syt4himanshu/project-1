# Recommended: omit VITE_API_URL so `/api` is same-origin (Vite proxy on :5173, nginx on :3005).
#
# Optional: API on another origin (must list this app's origin in backend ALLOWED_ORIGINS).
# VITE_API_URL=http://localhost:3005
