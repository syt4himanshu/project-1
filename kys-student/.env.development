# Leave VITE_API_URL unset so requests use same-origin `/api/...`:
# - Vite :5173 → `server.proxy` → backend
# - Nginx :3005 → `location /api/` → backend
#
# Only set this if the API is on another host (then ensure backend ALLOWED_ORIGINS includes this app's origin).
