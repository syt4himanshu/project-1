# Unified dev (repo root `npm run dev` + Nginx :3005): gateway origin only (no /api suffix; paths include /api/...)
VITE_API_URL=http://localhost:3005

# Single-app dev (Vite only): Vite `server.proxy` forwards /api to the backend
# VITE_API_URL=http://localhost:5174
