// Re-export chatbot-specific types from the shared api types for convenience
export type {
    ScopeMode,
    SectionKey,
    ParsedSections,
    ChatMessageModel,
    MenteeRow,
} from '../api/types'
